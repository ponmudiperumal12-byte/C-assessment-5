#include <stdio.h>
int main() {
    int a,count=0,s;
    printf("enter the number:");
    scanf("%d",&a);
    loop:for(a;a>0;a++)
    { 
        count++;
        a=(a /10);
        goto loop;
    }
    printf("%d\n", count);
}
