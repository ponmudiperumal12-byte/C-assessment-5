#include <stdio.h>
int main()
{
    int a, d, count = 0;
    printf(" enter the number:");
    scanf("%d", &a);
    for(a; a > 0; a = a/ 10)
    {
        d = a% 10;
        if(d % 2 != 0)
        {
            count++;
        }
    }

    printf("%d", count);
}
