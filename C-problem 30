#include <stdio.h>
int main()
{
    int a, b, c, i;
    printf ("enter the number:");
    scanf("%d%d%d", &a, &b, &c);
    for(i = 1; i > 0; i++)
    {
        if(i % a == 0 && i % b == 0 && i % c == 0)
        {
            printf("%d\n", i);
            break;
        }
    }
}
