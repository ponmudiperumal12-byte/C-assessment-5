#include <stdio.h>
int main() {
    int a,sum=0;
    printf("enter the number:");
    scanf("%d",&a);
    loop:for(a;a>0;a++)
    { 
        sum=sum+a%10;
        a=(a /10);
        goto loop;
    }
    printf("%d\n", sum);
}
