#include <stdio.h>
int main() {
    int a,rev=0;
    printf("enter the number:");
    scanf("%d",&a);
    loop:for(a;a>0;a++)
    { 
        rev=rev*10+a%10;
        a=(a /10);
        goto loop;
    }
    printf("%d\n", rev);
}
